# Integers

Integers in Cicero are *arbitrary-width*. That means that there isn't a limited
bit width to an integer value. You can make integers as big as your heart
desires (and your computer handles).

The operations available to integers are:
* addition `+`
* subtraction `-`
* multiplication `*`
* integer division `/`
* exponentiation `^`

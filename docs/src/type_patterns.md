# Type patterns

Cicero includes *type patterns* which allows users to specify information about
expected types at various points in the program. Note that this does not imply
that Cicero is *statically-typed*; even in obvious cases where the types are
wrong, type errors are not reported until the evaluation of those malformed
expressions.

func fib(n) -> {
  if n = 0 or n = 1 then {
    1;
  } else {
    fib(n - 1) + fib(n - 2);
  };
};
--func fib(n) -> {
--  func fib_helper(n, a, b) -> if n = 0 then a else fib_helper(n - 1, b, a + b);
--  fib_helper(n, 0, 1);
--};
fib(31);

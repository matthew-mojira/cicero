(set x 1)
(while true {
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
       (println x)
       (set x (+ x 1))
 })

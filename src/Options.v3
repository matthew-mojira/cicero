component CompileOptions {
	// TODO refactor assertions to be guarded by sanity checks (so that
	// disabling them with -O3 eliminates those checks from being compiled)
	// This will also allow us to insert many more sanity checks around the
	// program which we can disable to avoid overhead in stabler builds.
	def sanityChecks = true;
}

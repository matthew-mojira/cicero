/* The single Object of the unit type.
 */
class UnitObject extends Object {
	def display() -> string { return "()"; }
	def isTrue()  -> bool   { return false; }

	/* METHODS */

	def u_display() => Result.OK(StrObjects.fromString(display()));
}

component UnitObjects {
	/* only one U and one nit */
	def instance: Object = UnitObject.new();
}
